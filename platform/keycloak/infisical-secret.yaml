# Synced into namespace keycloak by platform-keycloak Application.
# Requires Secret infisical-universal-auth in infisical-operator-system.
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: keycloak-admin
  namespace: keycloak
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  hostAPI: http://192.168.68.10:8090/api
  syncConfig:
    resyncInterval: 60s
  authentication:
    universalAuth:
      secretsScope:
        projectSlug: kubernetes
        envSlug: prod
        secretsPath: /keycloak
        recursive: false
      credentialsRef:
        secretName: infisical-universal-auth
        secretNamespace: infisical-operator-system
  managedKubeSecretReferences:
    - secretName: keycloak-admin
      secretNamespace: keycloak
      creationPolicy: Owner
      template:
        includeAllSecrets: false
        data:
          admin-password: "{{ .KEYCLOAK_ADMIN_PASSWORD.Value }}"
---
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: keycloak-db
  namespace: keycloak
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  hostAPI: http://192.168.68.10:8090/api
  syncConfig:
    resyncInterval: 60s
  authentication:
    universalAuth:
      secretsScope:
        projectSlug: kubernetes
        envSlug: prod
        secretsPath: /keycloak
        recursive: false
      credentialsRef:
        secretName: infisical-universal-auth
        secretNamespace: infisical-operator-system
  managedKubeSecretReferences:
    - secretName: keycloak-db
      secretNamespace: keycloak
      creationPolicy: Owner
      template:
        includeAllSecrets: false
        data:
          password: "{{ .KEYCLOAK_DB_PASSWORD.Value }}"
