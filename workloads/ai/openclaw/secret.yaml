# OpenClaw secrets are NOT applied from Git (plain values previously leaked here).
# Create via lab-home-k8s/scripts/apply-bootstrap-secrets.sh:
#   kubectl -n ai-tools create secret generic openclaw-secrets \
#     --from-literal=OPENCLAW_GATEWAY_TOKEN=... \
#     --from-literal=LITELLM_API_KEY=...
